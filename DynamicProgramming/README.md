# Pattern: Dynamic Programming

This directory serves as an execution ledger for problems solved using the Dynamic Programming pattern. 

> **The 15-Minute Rule:** If core logic is not cracked within 15 minutes, the problem is marked as a fail/review. 
> **Complexity Rule:** Time and Space complexity must be stated before coding.

## 📝 Problem Autopsy Ledger

| # | Problem & Link | Time | Pattern & Triggers | Complexity | The "Gotcha" / Bottleneck | Edge Cases |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- |
| 1 | [5. Longest Palindromic Substring](https://leetcode.com/problems/longest-palindromic-substring/) | 14m logic<br>35m total | **Pattern:** Expand from the middle<br>**Trigger:** "Palindromic **substring**" | **Time:** `O(n^2)`<br>**Space:** `O(1)` | While this can be done using a 2D DP array to check palindrome in O(1) time getting a total time complexity of O(n^2) as well, it is practically slow due to space allocation. Since checking palidrome depends only one the boundary characters and if the string in middle is palindrome then you don't need the whole 2D DP array. Choose a center and expand from the middle until the substring is not a palindrome, and do this for all possible centers. Remember that even size strings have two centers unlike odd. | smallest palindromic substring is a character |
| 2 | [698. Partition to K Equal Sum Subsets](https://leetcode.com/problems/partition-to-k-equal-sum-subsets/description/) | 15m logic<br>40m total | **Pattern:** Bitmasking DP<br>**Trigger:** small n, "partition into subsets",  | **Time:** `O(n*2^n)`<br>**Space:** `O(2^n)` | To form all subsets of same size, you can use recursion to create k subsets. In order to keep track of elements used so far, you can use an boolean array visited. Since n is small you can easily use a bitmask instead. Memoize the bitmask so that each sequence is calculated only once. You can memoize the mask here because if a mask fails once, it will fail every time | If you can have required number of subsets(k) you have a valid partition |
| 3 | [70. Climbing Stairs](https://leetcode.com/problems/climbing-stairs/) | 3m logic<br> 7m total | **Pattern:** 1D DP<br>**Trigger:** "how many distinct ways"  | **Time:** `O(n)`<br>**Space:** `O(1)` | Number of ways to reach step n is the sum of number of ways to reach step n-2 and n-1 | Number of ways to reach step 1 is 1 and step 0 we can assume as 1 |
| 4 | [746. Min Cost Climbing Stairs](https://leetcode.com/problems/min-cost-climbing-stairs/) | 3m logic<br> 7m total | **Pattern:** 1D DP<br>**Trigger:** "minimum cost", "current state depends on previous states"  | **Time:** `O(n)`<br>**Space:** `O(1)` | Minimum cost to reach the top is the minimum of the cost to leave from one step below or two step below | minimum cost to leave step 0 is cost[0] and minimum cost to leave step 1 is cost[1] |
| 5 | [198. House Robber](https://leetcode.com/problems/house-robber/description/) | 4m logic<br> 7m total | **Pattern:** 1D DP<br>**Trigger:** "maximum amount", "current state depends on previous states"  | **Time:** `O(n)`<br>**Space:** `O(1)` | Either rob the last house or the second last house. Amount for robbing house n = nums[n] + max(profit[n-2], profit[n-3])| if there's only one house rob it |
| 6 | [213. House Robber II](https://leetcode.com/problems/house-robber-ii/description/) | 5m logic<br> 23m total | **Pattern:** 1D DP<br>**Trigger:** "maximum amount", "current state depends on previous states"  | **Time:** `O(n)`<br>**Space:** `O(1)` | House Robber but with two cases, either consider house 0 to n-2 or 1 to n-1 | if there's only one house rob it |
| 7 | [740. Delete and Earn](https://leetcode.com/problems/delete-and-earn/) | 16m logic<br> 23m total | **Pattern:** 1D DP<br>**Trigger:** "maximmize the number of points", "current state depends on previous states"  | **Time:** `O(n*logn)`<br>**Space:** `O(n)` | For each distinct point you can either take the point before it(in sorted order) or not take it. If the point before it is 1 less then you definitely do not take it | if there's only one point, take it |
| 8 | [322. Coin Change](https://leetcode.com/problems/coin-change/description/) | 13m logic<br> 69m total | **Pattern:** Unbounded Knapsack<br>**Trigger:** "fewest number of coins", "current state depends on previous states"  | **Time:** `O(amount*n)`<br>**Space:** `O(amount)` | Typical Unbounded knapsack approach, build an array with indices as all possible values and keep updating the array until you have performed the max possible operations and then check if you reached the target, sort the coins so that if at any point the amount is invalid you don't need to check further | for 0 amount you need 0 coins |
| 9 | [279. Perfect Squares](https://leetcode.com/problems/perfect-squares/description/) | 2m logic<br> 17m total | **Pattern:** Unbounded Knapsack<br>**Trigger:** "least number of perfect squares required", "current state depends on previous states"  | **Time:** `O(n*sqrt(n))`<br>**Space:** `O(n)` | Typical Unbounded knapsack approach, build an array with indices as all possible values and keep updating the array until you have performed the max possible operations and then check if you reached the target,  | for 0 amount you need 0 perfect squares |
| 10 | [121. Best Time to Buy and Sell Stock](https://leetcode.com/problems/best-time-to-buy-and-sell-stock/description/) | 2m logic<br> 8m total | **Pattern:** 1D DP <br>**Trigger:** "maximize your profit", "current state depends on previous states"  | **Time:** `O(n)`<br>**Space:** `O(1)` | For any day, the maximum profit that can be obtained on that day depends the minimum buying price among the past days | On first day the profit will always be zero. |
| 11 | [122. Best Time to Buy and Sell Stock II](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/) | 15m logic<br> 19m total | **Pattern:** 1D DP <br>**Trigger:** "maximize your total profit", "current state depends on previous states"  | **Time:** `O(n)`<br>**Space:** `O(1)` | For any day you can either choose to sell, buy or do nothing. The profit on that day `i` if you are selling would depend on the max profit made until the day before given that you have bought a stock, similarly, the profit on a day `i` if you are buy would depend on the max profit made until the day before by selling. Alternatively, you can choose to do nothing  | On the first day it is impossible to sell hence buy is set to `-1e7` |
| 12 | [309. Best Time to Buy and Sell Stock with Cooldown](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/description/?q=buy+and+sell) | 5m logic<br> 10m total | **Pattern:** 1D DP <br>**Trigger:** "maximum profit you can achieve", "current state depends on previous states"  | **Time:** `O(n)`<br>**Space:** `O(n)` | For any day you can either choose to sell, buy or do nothing. The profit on that day `i` if you are selling would depend on the max profit made until the day before given that you have bought a stock, similarly, the profit on a day `i` if you are buy would depend on the max profit made until the day before yesterday by selling. Alternatively, you can choose to do nothing  | On the first day it is impossible to sell hence buy is set to `-1e4` |
| 13 | [152. Maximum Product Subarray](https://leetcode.com/problems/maximum-product-subarray/description/) | 7m logic<br> 16m total | **Pattern:** 1D DP <br>**Trigger:** "subarray with largest product", "current state depends on previous states"  | **Time:** `O(n)`<br>**Space:** `O(1)` | Maximum possible product for a subarray ending at index `i` depends on both the minimum and maximum product of subarray ending at `i-1` | For an array with one element, that is the maximum product |
| 14 | [139. Word Break](https://leetcode.com/problems/word-break/) | 15m logic<br> 40m total | **Pattern:** 2D DP or Unbounded Knapsack <br>**Trigger:** "Substrings", "Use words in dictionary any number of times", "current state depends on previous states"  | **Time:** `O(n^2 * l)`<br>**Space:** `O(n + k*l)` | You can either build all possible strings using the word in dictionary of size at most `n` and see if target string is generated OR at any index `i` check if there is a word forming being `i` in target string that is also present in the dictionary and the substring before that can be broken using the dictionary (almost like LIS approach) | Empty string does not need to be broken and will give us true as it can also be broken using 0 words from dictionary |
| 15 | [300. Longest Increasing Subsequence](https://leetcode.com/problems/longest-increasing-subsequence/description/) | 1m logic<br> 7m total | **Pattern:** LIS <br>**Trigger:** "Longest strictly increasing subsequence", "current state depends on previous states"  | **Time:** `O(n^2)`<br>**Space:** `O(n)` | Standard LIS approach, at any index `i` check the indices before it to find a number smaller. Among such numbers choose the one that has the longest subsequence so far | You will always have a subsequence of size 1 |
| 16 | [416. Partition Equal Subset Sum](https://leetcode.com/problems/partition-equal-subset-sum/) | 11m logic<br> 20m total | **Pattern:** 0/1 Knapsack <br>**Trigger:** "partition into two subsets", "small input size"  | **Time:** `O(n*sum)`<br>**Space:** `O(sum)` | To make one subset, you can either take an element or leave it. The left elements will form the second subset. Return true is the sum of one subset is `(totalSum)/2` | Taking zero elements will result in zero sum |
| 17 | [494. Target Sum](https://leetcode.com/problems/target-sum/description/) | 35m logic<br> 46m total | **Pattern:** 0/1 Knapsack <br>**Trigger:** "assign '+' to some and '-' to others", "small input size"  | **Time:** `O(n*targetSum)`<br>**Space:** `O(targetSum)` | This is essentially asking you to partition into two subsets and count the number of ways to reach a target sum. The target mentioned can be reached if the sum of one subset is `(totalSum + target)/2` | Taking zero elements will result in zero sum and there is only one way to reach zero sum |
| 18 | [1143. Longest Common Subsequence](https://leetcode.com/problems/longest-common-subsequence/description/) | 8m logic<br> 13m total | **Pattern:** String DP/ 2D DP <br>**Trigger:** "two string", "length of longest common subsequence"  | **Time:** `O(n*m)`<br>**Space:** `O(n*m)` | Build this bottom-up, the state is length longest common subsequence between text1[0:i] and text2[0:j]. If text1[i] and text2[j] are same then length is 1 + dp[i-1][j-1] otherwise it would be max of dp[i-1][j] and dp[i][j-1] | For zero length string, length of LCS is 0 |